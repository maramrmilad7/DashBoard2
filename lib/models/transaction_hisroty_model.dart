class TransactionHisrotyModel {
  final String amount, date, title;
  final bool isWithDrawer;

  const TransactionHisrotyModel( {
    required this.amount,
    required this.date,
    required this.title,
    required this.isWithDrawer,
  });
}
